[project]
name = "sample-api"
version = "0.1.0"
description = "Sample API deployed on sitesolide"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
]

[tool.uv]
package = false
